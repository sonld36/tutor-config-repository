server:
  port: 0
  servlet:
    context-path: /course

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tutor-system
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        show_sql: true